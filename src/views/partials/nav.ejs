<nav
  class="nav"
  style="
    display: flex;
    background-color: beige;
    gap: 12px;
    align-items: center;
    padding: 10px 0;
    border-bottom: 1px solid #eee;
    margin-bottom: 12px;
  "
>
  <a href="/">Inicio</a>

  <% /* Links “de trabajo” (solo si estás logueado) */ %> <% if (user) { %>
  <a href="./admin">Admin</a>
  <a href="/imprimir-qr">Imprimir QR</a>
  <% } %>

  <span style="flex: 1"></span>

  <% if (user) { %>
  <span style="opacity: 0.7">Hola, <%= user.username %></span>
  <a href="/logout">Salir</a>
  <% } else { %>
  <a href="/login">Entrar</a>
  <% } %>
</nav>
